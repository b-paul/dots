#! /usr/bin/env bash

set -e

source "$(dirname $0)/cdotfiles"

for file in $(git ls-tree -r HEAD --name-only); do
  mkdir -p "$HOME/$(dirname $file)"
  ln -s "$PWD/$file" "$HOME/$file" || true
done

