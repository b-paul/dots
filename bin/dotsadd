#! /usr/bin/env bash

set -e

source "$HOME/bin/cdotfiles"

for file in $@; do
  sourceloc="$HOME/$file"
  targetloc="$PWD/$file"
  if [ -f $sourceloc ]; then
    mkdir -p "$(dirname $targetloc)"
    cp "$sourceloc" "$targetloc" && ln -sf "$targetloc" "$sourceloc" || rm "$targetloc"
  fi
done

