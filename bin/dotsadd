#! /usr/bin/env bash

SOURCE=$BASH_SOURCE
while [ -h $SOURCE ]; do
  NEW_SOURCE="$(readlink $SOURCE)"
  [[ $NEW_SOURCE != /* ]] && NEW_SOURCE="$(dirname $SOURCE)/$NEW_SOURCE"
  SOURCE=$NEW_SOURCE
done

cd "$(dirname $SOURCE)"
cd "$(git rev-parse --show-toplevel)"

for file in $@; do
  fileloc="$HOME/$file"
  if [ -a $fileloc ]; then
    cp "$fileloc" "./$file" && ln -s "$PWD/$file" "$fileloc"
  fi
done

